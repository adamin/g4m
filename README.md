# Consignments management system

## Usage

### Starting a new batch

`$batch = new ConsignmentCollection();`

### Adding new consignment

`$batch->add($consignment);`

### Ending current batch

`$batch->end();`